{
  "version": 6,
  "cmakeMinimumRequired": {
    "major": 3,
    "minor": 24,
    "patch": 0
  },
  "configurePresets": [
    {
      "name": "windows-debug",
      "displayName": "Windows Debug",
      "description": "Debug configuration for Windows using the default CMake generator",
      "binaryDir": "${sourceDir}/build/windows-debug"
    },
    {
      "name": "windows-release",
      "displayName": "Windows Release",
      "description": "Release configuration for Windows using the default CMake generator",
      "binaryDir": "${sourceDir}/build/windows-release"
    }
  ],
  "buildPresets": [
    {
      "name": "windows-debug",
      "configurePreset": "windows-debug",
      "configuration": "Debug"
    },
    {
      "name": "windows-release",
      "configurePreset": "windows-release",
      "configuration": "Release"
    }
  ]
}
